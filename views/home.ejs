<%- include('partials/header'); -%>

    <h1>Home</h1>
    <p> <%= startingContent %> </p>

    <% postArray.forEach(element => { %>
      <h1>  <%= element.title %> </h1>
      <p>  <%= element.post.slice(0, 100) + "..." %> 
           <a href= "/posts/<%= element.title %>" + > Read More </a>
      </p>
    <% }); %>

<%- include('partials/footer'); -%>